# FRONTEND
- Framework: React (via Vite)
- Language: JavaScript / JSX
- Styling: I dont Like In line css, so youe separate .css file on each .jsx file
- Routing: React Router DOM (for the 2 pages).

# BACKEND / ENGINE
- API Server: Python with Flask & Flask-CORS.
- Lexical Engine: C and Flex (Lex). Executed via Python's `subprocess` module.

# PACKAGE MANAGERS
- Frontend: `npm`
- Backend: `pip`