# STATE MANAGEMENT
1. Tooling: Use standard React `useState` and `useEffect`. 
2. STRICT RULE: Do NOT introduce Redux, Zustand, React Query, or Context API. The state for this app is trivially simple (just `code` input string, `output` string, and a `loading` boolean). Keep it strictly local to the components.